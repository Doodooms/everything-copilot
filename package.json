{
  "name": "agentic-workflow",
  "private": true,
  "description": "Deterministic GitHub Copilot workflow repository for skills, agents, prompts, instructions, hooks, and MCP tooling.",
  "engines": {
    "node": ">=22.5.0",
    "npm": ">=9"
  },
  "scripts": {
    "ahk:health": "bash health.sh",
    "ahk:status": "npx --no-install ahk status",
    "ahk:sync": "npx --no-install ahk sync --direction in",
    "ahk:dashboard": "npx --no-install ahk dashboard --no-open",
    "docker:build": "docker build -t agentic-workflow:latest .",
    "docker:test:workspace:stability": "docker run --rm agentic-workflow:latest bash -lc \"npm run test:workspace:stability\"",
    "docker:run": "docker run --rm -it agentic-workflow:latest",
    "lint:create-surfaces": "uv run python scripts/customization_lint.py create-surfaces --root .",
    "test:ahk:e2e": "uv run pytest tests/test_ahk_integration.py -q",
    "test:graphify:fts": "uv run pytest tests/test_atomic_index_fts.py -q",
    "test:workspace:stability": "bash health.sh && npx --no-install ahk sync --direction in && npx --no-install ahk status && uv run pytest tests/test_ahk_integration.py tests/test_atomic_index_fts.py -q"
  },
  "devDependencies": {
    "@cardor/agent-harness-kit": "^1.1.2"
  }
}
