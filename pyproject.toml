[project]
name = "agentic-workflow"
version = "0.1.0"
description = "Deterministic GitHub Copilot workflow repository for skills, agents, prompts, and MCP tooling."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.27.1",
    "pyyaml>=6.0.3",
    "typer>=0.25.1",
    "graphifyy>=0.7.11",
    "ladybug>=0.16.1",
]

[tool.uv]
package = false
